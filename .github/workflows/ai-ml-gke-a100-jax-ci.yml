name: ai-ml-gke-a100-jax-ci
on:
  push:
    branches:
      - main
    paths:
      - '.github/workflows/ai-ml-ci.yml'
      - 'ai-ml/gke-a100-jax/**'
  pull_request:
    paths:
      - '.github/workflows/ai-ml-ci.yml'
      - 'ai-ml/gke-a100-jax/**'
jobs:
  job:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v3
      - name: build tensorflow container
        run: |
          cd ai-ml/gke-a100-jax
          docker build --tag tensorflow .
