apiVersion: apps/v1
kind: Deployment
metadata:
  name: vllm-gemma-deployment
spec:
  template:
    metadata:
      labels:
        ai.gke.io/model: llama-2-7b-gptq
    spec:
      containers:
      - name: inference-server
        args:
        - --model=$(MODEL_ID)
        - --tensor-parallel-size=1
        - --quantization=gptq
        env:
        - name: MODEL_ID
          value: dganochenko/Llama-2-7B-GPTQ

